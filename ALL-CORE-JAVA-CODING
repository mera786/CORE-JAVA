(1) Override the equals() and hashCode() methods in a class Book having fields: isbn, title, and author. 
    Two books are considered equal if they have the same isbn.?
Input:
        Book book1 = new Book("9781234567897", "Java Programming", "Meraz Alam");
        Book book2 = new Book("9781234567897", "Java Programming", "Meraz Alam");
        Book book3 = new Book("9781234567898", "Python Programming", "Meraz Alam");

(2) Create a Student class with id, name, and marks.Implement the Comparable interface so that students are sorted by marks in ascending order.then print names?
Input: 
List<Student> students = Arrays.asList(
    new Student(101, "Alice", 85),
    new Student(102, "Bob", 70),
    new Student(103, "Charlie", 92)
);
Output: [Bob, Alice, Charlie]

(3) Given a list of Employee objects with fields id, name, and salary,write separate comparators to sort: (a) By salary (descending) and print name, (b) By name (alphabetical) and print name?
Input:
List<Employee> employees = Arrays.asList(
    new Employee(201, "Zara", 55000),
    new Employee(202, "Adam", 70000),
    new Employee(203, "Mona", 62000)
);
✅ Output :
(Sorted by salary descending) -> [Adam, Mona, Zara]
(Sorted by name ascending ) -> [Adam, Mona, Zara]

(4) You are given an ArrayList of integers. You are iterating over it using an iterator. During iteration, you try to add a new element to the list using list.add(50). What will be the output?
Input: 
List<Integer> list = new ArrayList<>(Arrays.asList(10, 20, 30, 40));
Output: 
ConcurrentModificationException

(5) You are given a CopyOnWriteArrayList of strings. You iterate through it and during iteration, you add a new element "John". What will be the final list?
Input:
CopyOnWriteArrayList<String> list = new CopyOnWriteArrayList<>(List.of("Alice", "Bob"));
Output:
During iteration: Alice Bob 
Final list after iteration: Alice Bob John

(6) Write a Java program to create a thread using both Thread and Runnable.?

(7) You have two classes — one that extends Thread and one that implements Runnable. Write code to start both threads. Explain the difference in how start() and run() behave in both cases.?

(8) Write program to show life cycle of thread ?

(9) write program to show usage of these method? run,start,sleep,join,setPriority,interrupt ?

() You are given a scenario with three threads:
  -> Thread A should print "A" every 1 second using sleep().
  -> Thread B should print "B" many times, but it should give other threads a chance to run using yield().
  -> Thread C should not start printing immediately. It should wait for the main thread to give it a signal, then it will print "C" five times, 
     waiting 500 milliseconds between each print. Use wait() and notify() for this.?








